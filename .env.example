# Server Configuration
PORT=8000
HOST=0.0.0.0

# LLM Providers Configuration (Optional for server-side key fallback. Users can bring their own keys)
# GROQ_API_KEY=gsk_...
# OPENAI_API_KEY=sk-...

# Database Connection (Defaults to SQLite in backend/data/lawed.db)
# DATABASE_URL=sqlite:///backend/data/lawed.db
